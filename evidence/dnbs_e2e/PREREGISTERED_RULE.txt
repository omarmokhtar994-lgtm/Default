Registered 2026-09-24T05:02:38Z, before any end-to-end DNBS run.

Change: day-neighbourhood break search (DNBS), a late phase after RC8 quality
recovery that re-places one day's breaks at a time on the two strongest
compliant skeletons and only adds candidates that dominate their anchor on the
engine's own metrics and pass the release validator.

Arms: CURRENT = repo HEAD engine (a66f5c6); NEW = HEAD + DNBS.
Config: production runner, FULL_SCHEDULE, QUICK, --time-limit 900, --num-workers 2,
two runs concurrently (4 cores), seed 9000.
Cases: Cricut Chat, Cricut Voice, AE_AR_B2B, NMG_SP (real); SYNTH_M2, SYNTH_H3,
SYNTH_H1 (planted).

SHIP iff ALL:
  (1) every NEW run: validator PASS, 0 hard failures, parity PASS;
  (2) summed after_target NEW >= summed CURRENT;
  (3) no REAL workbook worse by more than 2 intervals. If one is, that case is
      rerun at seed 9001 in both arms and judged on the two-seed mean;
  (4) no run of NEW produces no schedule where CURRENT produced one.
Otherwise NOT shipped.
